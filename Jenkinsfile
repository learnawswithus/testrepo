pipeline {
       agent any
       stages {
              stage ('build') {
              echo "This is a build stage"'
              }
              stage ('Manual approval') {
                     input " Deploy approval ? "
              }
              stage ('deploy') {
              'echo "This is a deploy stage"'
              }
              
          }
