pipeline {
       agent any
       stages {
              stage ('build') {
              sh 'echo "This is a build stage"'
              }
              stage ('deploy') {
              sh 'echo "This is a deploy stage"'
              }
              
          }
